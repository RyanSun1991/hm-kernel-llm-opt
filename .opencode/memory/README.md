# Long-Term Memory

Use this directory for stable, reusable knowledge that should survive individual pipeline runs.

## Structure

- `targets/`: file or directory specific memory
- `subsystems/`: broader subsystem memory
- `global_lessons.md`: reusable lessons across the whole repository
