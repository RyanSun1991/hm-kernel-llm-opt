# Skill Packs

These are repo-local capability packs for OpenCode agents.

They are not tied to a vendor-specific skill API. They are reusable instruction packs that pipeline presets and starter agents can load by path.
