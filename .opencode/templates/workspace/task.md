# Task: <task-slug>

objective: <one sentence — what must become true>
scope: <files · subsystem · commit — the boundary of this task>
constraints:
  - <hard limits: ABI stability, approvals required, deadlines>
state: ready            # ready | running | waiting-user | done
opened: <YYYY-MM-DD>
opened_by: <role that opened it, on whose request>

## Notes

<anything about the task setup that does not belong in the capsule — links to the
originating request, related workspaces, prior art>
