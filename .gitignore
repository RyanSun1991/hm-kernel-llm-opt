# Python
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
.venv/

# Local data
/data/*
!/data/.gitkeep

# Logs
*.log

# IDE
.vscode/
.idea/

# OS
.DS_Store

# Docker local env
.env.docker

# Per-member local skill-hub state (sediment staging, in-flight memory) —
# design §8/promotion_policy: local-only, never committed; shared knowledge
# reaches the hub via staging/ PRs instead.
/.opencode/local/

# Hub-bridge read-path audit log — per-run record of what `hmopt resolve`
# mounted into agent context (.opencode/skills/infra/hub-bridge). Runtime-only.
/.opencode/state/retrieval.jsonl
